name: ci

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Don't run more than one of these in parallel on the same branch.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  backend:
    name: backend · pytest
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    env:
      # CI runs in dev mode with auth disabled so the existing tests
      # keep working without a token dance. Production deployments must
      # set these explicitly.
      APP_ENV: test
      AUTH_REQUIRED: "false"
      # Point the test suite at the real Postgres service so
      # Postgres-specific SQL (enums, timestamptz, array columns) is
      # exercised before merge. The app's own DATABASE_URL is still set
      # below for any code path that reads it directly.
      TEST_DATABASE_URL: postgresql+psycopg2://ci:ci@localhost:5432/music_ai_test
      DATABASE_URL: postgresql+psycopg2://ci:ci@localhost:5432/music_ai_test
      REDIS_URL: redis://localhost:6379/0
      JWT_SECRET: ci-jwt-secret-do-not-use-in-prod
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: ci
          POSTGRES_PASSWORD: ci
          POSTGRES_DB: music_ai_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U ci -d music_ai_test"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 5
    steps:
      - uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: |
            backend/requirements.txt
            backend/requirements-dev.txt
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          # Both runtime and test requirements.
          pip install -r requirements.txt
          pip install -r requirements-dev.txt
      - name: Run pytest
        env:
          # Tell passlib to use the bundled pure-python bcrypt backend
          # (the native extension is fragile on the GitHub runner).
          PASSLIB_BCRYPT_FORCE_BACKEND: pure-python
        run: pytest -q --tb=short --maxfail=3

  frontend:
    name: frontend · tsc + build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: Install dependencies
        run: npm ci
      - name: Type-check
        run: npx tsc --noEmit
      - name: Build
        # `npm run build` runs `tsc -b && vite build` so we get both
        # type errors and bundle errors in one step.
        run: npm run build
