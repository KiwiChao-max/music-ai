# Test & dev dependencies. Kept separate from `requirements.txt` so production
# images don't pull pytest, linters, etc.
pytest>=8.0
pytest-asyncio>=0.23

# Lint + format (Ruff replaces flake8, isort, pyupgrade, pydocstyle)
ruff>=0.8.0

# Static type checking
mypy>=1.13.0
types-passlib
