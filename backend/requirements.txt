fastapi>=0.110
uvicorn[standard]>=0.27
# Test client + async HTTP for the /api/* endpoint tests.
httpx>=0.27
pydantic>=2.5
pydantic-settings>=2.1
python-multipart>=0.0.9
SQLAlchemy>=2.0
alembic>=1.13
psycopg2-binary>=2.9

# ---- auth -----------------------------------------------------------------
# JWT signing / verification (HS256) and password hashing (bcrypt via passlib).
# `passlib[bcrypt]` ships the bcrypt backend; the version range stays
# compatible with the rest of the stack.
passlib[bcrypt]>=1.7.4
bcrypt<4.0
python-jose[cryptography]>=3.3
email-validator>=2.0

# ---- observability --------------------------------------------------------
# Prometheus client for the /metrics endpoint.
prometheus-client>=0.20

# Async task queue. `celery[redis]` pulls in the redis client transitively,
# but we pin it explicitly so the API process (which only needs to dispatch)
# installs cleanly even if a lighter `celery` extra is used elsewhere.
celery>=5.4
redis>=5.0

# ---- Audio To MIDI (Milestone 2) ----------------------------------------
# Spotify Basic Pitch: ONNX-based polyphonic audio-to-MIDI transcription.
# Pinned to a minor range that's known to install cleanly on Windows + Linux
# with the bundled ONNX runtime.
basic-pitch>=0.3,<0.5

# MIDI I/O. `pretty_midi` is used to re-load what Basic Pitch wrote (round
# trip + counting notes for the e2e test). `mido` is lighter and used for
# any future low-level edits (tempo, program change, ...).
pretty_midi>=0.2.10
mido>=1.3
librosa>=0.11
soundfile>=0.12

# ---- AI source separation ------------------------------------------------
# Meta Demucs: separates mixed audio into vocals / drums / bass / other.
# The worker falls back to placeholder stems if this dependency is absent,
# but production-quality stem MIDI needs it installed.
demucs>=4.0,<5
# WAV inputs run through the local Python API; FFmpeg/ffprobe is only needed for CLI fallback / compressed formats.
