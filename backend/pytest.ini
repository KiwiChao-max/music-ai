[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# The project doesn't ship an async API yet, but enabling asyncio mode here
# keeps the option open for future worker tests without a config change.
asyncio_mode = auto
